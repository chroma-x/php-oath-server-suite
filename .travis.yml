language: php

before_script:
  - printf "\n" | pecl install imagick

branches:
  only:
    - master

php:
  - '5.3'
  - '5.4'
  - '5.5'
  - '7.0'
  - hhvm

install:
  - composer install

matrix:
  allow_failures:
    - php: hhvm
